# Это переводчик между разными коробочками данных.
# DTO ↔ Сущность
# Сущность ↔ модель БД
# 👉 Без маппера было бы путаница: в базе поля называются так, в сущности — по-другому, в ответе API — ещё иначе.